export const TYPE = 'GET'
export const sum = function(x: number, y: number):number {
    return x + y
}
export default function threeSum(x: number, y: number, z: number): number {
    return x + y + z
}
